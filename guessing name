import random 

random_num = random.randint (1,100)

gusse = int(input("enter gusse"))

while True: 

    if random_num > gusse:
        print ("gusse lower")
        
    if random_num < gusse:
        print ("gusse higher")
        
    if random_num == gusse:
        print ("Correctr")

restart 
